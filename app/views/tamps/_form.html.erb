<%= form_for @tamp, :html => { :class => 'form-horizontal', :multipart => true } do |f| %>

  <div class="form-actions">
  	<%= f.file_field :file %>
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tamps_path, :class => 'btn' %>
  </div>
<% end %>
